Dekho apan chaah to ise list/dll se bana skte h bcs obv usme already ye front,back and all fn exists krte h.
But we will do it with ARRAY (circular fomat) ->
MANTRAS ->
INCREEMENT -> (+1)%k
DECREEMENT -> (-1+k)%k

where k=size of array!

ab baaki to figure out krhiloge u just need two pointers front and rear!
baaki ek var to know abhi tak kitne elemenets dale to check if full or not and ek vector ofc!


class MyCircularDeque {
public:


    vector<int> deq;
    int K;
    int front,rear;
    int currCount;

    MyCircularDeque(int k) {
        K = k;
        deq = vector<int>(K,0);
        front = 0;
        rear = k-1;
        currCount = 0;
    }
    
    bool insertFront(int value) {
        if(isFull()){
            return false;
        }

        front = (front-1+K)%K; //are bhai abhi kuch front pe daala to front to hamesha most front ko point krna chhiyena to use piche jana pdega
        deq[front] = value;

        //hum pehle hi front pointer piche lelelere h taaki front pe humesha jo value h vo point kre! kyuki fir getfront me vrna deq[front-1+K%K] krna pdega bcs acc to this logic front to empty ko point krra isliye pehle fornt ko piche ledenge
        currCount++;
        return true;
    }
    
    bool insertLast(int value) {
        if(isFull()){
            return false;
        }

        rear = (rear+1)%K;
        currCount++;
        deq[rear] = value;
        return true;
    }
    
    bool deleteFront() {
        if(isEmpty()) return false;

        
        //ab front wala del kiya to front vala pointer ab khi point bhi krna chahiyena to apne se aage wale ko hi point krega na!
        front = (front+1)%K;
        currCount--;
        return true;
    }
    
    bool deleteLast() {
        if(isEmpty()) return false;

        
        rear = (rear-1+K)%K; //are bhai tune del krdiya rear ko to vo piche wale rear ko hi to point kregana ab!
        currCount--;
        return true;
    }
    
    int getFront() {
        if(isEmpty()) return -1;
        return deq[front];
    }
    
    int getRear() {
        if(isEmpty()) return -1;
        return deq[rear];
    }
    
    bool isEmpty() {
        return currCount == 0;
    }
    
    bool isFull() {
        return currCount == K; 
    }
};

